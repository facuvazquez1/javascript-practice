// Funciones avanzafas 

// Ciudadanos de primera clase 

const saludo = function (nombre) {
    console.log(`Hola ${nombre}`)
}
saludo("Facundo") // Resultado: Hola facundo


function procesarSaludo(saludoFunction, nombre){
    saludoFunction(nombre)
}

procesarSaludo(saludo, "Marcos") // Resultado: Hola Marcos
